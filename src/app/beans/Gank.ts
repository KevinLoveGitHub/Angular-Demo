export class Gank {
  // _id: string;
  // createdAt: string;
  // desc: string;
  // publishedAt: string;
  // source: string;
  type: string;
  url: string;
  // used: string;
  // who: string;


  constructor(url: string, type: string) {
    this.url = url;
    this.type = type;
  }
}
